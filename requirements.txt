# Core dependencies
yfinance>=0.2.38
pandas>=2.2.0
numpy>=1.26.4

# Technical analysis
# TA-Lib은 윈도우에서 pip로 바로 설치되지 않을 확률이 높습니다.
# 만약 에러가 나면 이 줄을 주석처리(#)하고 실행하세요.
# ta-lib==0.4.28
# 에러 해결: 버전을 지정하지 않거나 최신 베타 버전을 허용합니다.
pandas-ta>=0.3.14b

# Data visualization
matplotlib>=3.8.3
plotly==5.19.0

# Database
sqlalchemy==2.0.27

# Utilities
python-dotenv==1.0.1
pydantic>=2.6.1
pydantic-settings>=2.1.0

# Logging
loguru==0.7.2

# Async support
aiohttp==3.9.3
# 주의: asyncio는 파이썬 내장 라이브러리입니다. 
# 별도로 설치할 필요가 없으며, 버전 지정 설치 시 오류가 날 수 있습니다.
# asyncio==3.4.3 (삭제 권장)

# Testing
pytest==8.0.0
pytest-asyncio==0.23.5
pytest-cov==4.1.0
